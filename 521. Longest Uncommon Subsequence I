class Solution {
public:
    int findLUSlength(string a, string b) {
        // Approach 2
        return a == b? -1 : max(a.size(), b.size());
        
        
        /*
        // Approach 1
        if(a == b)
            return -1;
        return max(a.size(), b.size());
        */
    }
};
